TDM MANUFACTURING V12.2 ACCOUNT FIX

1. Deploy all files in this ZIP to the same GitHub Pages site, replacing the old files.
2. In Supabase SQL Editor, run:
   SUPABASE-V12.2-ACCOUNT-NOTIFICATION-PROFILE-FIX.sql
3. Refresh the app. If an old PWA/service-worker copy appears, close the site and reopen it; the cache version has been bumped to V12.2.

FIXED:
- Customer Account Settings now saves name, phone, home address and delivery address through Supabase Auth metadata.
- Profile picture can upload to the TDM media bucket under the signed-in customer's own profile folder.
- Profile picture URL is saved in the customer's Auth metadata and shown on the account dashboard.
- Notifications page now has a clear database error instead of silently claiming notifications are disabled.
- New orders automatically create a customer notification.
- Order status changes automatically create a customer notification.
- Payment status changes automatically create a customer notification.
- Notifications update live through Supabase Realtime when enabled.
- Password change has its own status message.
